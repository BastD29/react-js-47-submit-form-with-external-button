function MyButton() {
  return (
    // make the button form attribute correspond to the form id attribute (myForm)
    <button type="submit" form="myForm">
      Submit
    </button>
  );
}

export default MyButton;
